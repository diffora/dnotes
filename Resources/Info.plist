<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleName</key>                 <string>dnotes</string>
    <key>CFBundleDisplayName</key>          <string>dnotes</string>
    <key>CFBundleIdentifier</key>           <string>com.diffora.dnotes</string>
    <key>CFBundleExecutable</key>           <string>dnotes</string>
    <!-- Generated from images/icon.png by scripts/bundle.sh. Finder and
         notifications only: the menu bar item draws its own template glyph. -->
    <key>CFBundleIconFile</key>             <string>AppIcon</string>
    <key>CFBundlePackageType</key>          <string>APPL</string>
    <!-- Edit this by hand. PlistBuddy rewrites the file and drops every comment
         below, including the one that explains LSUIElement. -->
    <key>CFBundleShortVersionString</key>   <string>0.4.0</string>
    <key>CFBundleVersion</key>              <string>1</string>
    <key>LSMinimumSystemVersion</key>       <string>14.0</string>
    <key>NSHighResolutionCapable</key>      <true/>
    <!-- Only how the process starts. The Dock icon is a setting now
         (SettingsStore.showsDockIcon, off by default); main.swift promotes the app to
         .regular when the setting asks for it. True here so the default case never
         flashes a Dock icon on launch. -->
    <key>LSUIElement</key>                  <true/>
</dict>
</plist>
